# Whether an xcms object supports and currently has feature definitions. Plain MsExperiment has no hasFeatures method; only XcmsExperiment / XCMSnExp.

Whether an xcms object supports and currently has feature definitions.
Plain MsExperiment has no hasFeatures method; only XcmsExperiment /
XCMSnExp.

## Usage

``` r
.xcms_has_features(x)
```
