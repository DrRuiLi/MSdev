# MS instrument description from an MSdev object

Returns manufacturer and model from `projectInfo`, with placeholder
values replaced by readable defaults when unknown.

## Usage

``` r
get_MSdev_instrument(object)
```

## Arguments

- object:

  An `MSdev` object.

## Value

Character string `"<manufacturer> <model>"`.
